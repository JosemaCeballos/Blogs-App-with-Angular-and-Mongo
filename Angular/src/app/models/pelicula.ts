export class Pelicula {
    /*
    public year: number;
    public title: string;
    public image: string;

    constructor(year: number, title: string, image: string) {
        this.year = year;
        this.title = title;
        this.image = image;
    }
    */
    constructor(
        public year: number,
        public title: string,
        public image: string,
    ) { }
}